twilio:
  token: ${TWILIO_TOKEN}
  sid: ${TWILIO_SID}

spring:
  datasource:
    url: ${DATASOURCE_URL}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
  cloud:
    gcp:
      project-id: ecstatic-branch-366221
      firestore:
        project-id: ecstatic-branch-366221
        credentials:
          location-file: C:\Users\14bee\Documents\thriftydbkey.json

phone:
  admin: ${ADMIN_PHONE}
  developer: ${DEVELOPER_PHONE}
  service: ${SERVICE_PHONE}